 <h2>Main Page</h2>

 <form asp-action="Main" method="post">
     <button type="submit" name="role" value="Lecturer">Lecturer</button>
     <button type="submit" name="role" value="AcademicManager">Academic Manager</button>
     <button type="submit" name="role" value="ProgrammeCoordinator">Programme Coordinator</button>
 </form>

 @if (ViewData["ShowTable"] != null && (bool)ViewData["ShowTable"])
 {
     <h3>Claims Table</h3>
     <table>
         <!-- Your code to display the claims table -->
     </table>
 }
